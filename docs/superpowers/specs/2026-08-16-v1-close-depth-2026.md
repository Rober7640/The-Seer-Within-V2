# V1 close depth — 132 words vs 431 (`v1_close_depth_2026`)

**Status: BUILT and dark (2026-08-16).** Code merged behind an experiment that does not
exist yet, so every woman still reads today's close. Remaining steps, in order:

1. deploy
2. run `improve-v1/create-close-depth-experiment-2026-08-16.sql` — dev first, then prod
3. re-check `targetN` against the live baseline, then press Start in `/admin/experiments`

**Blocked by:** nothing. Reordered ahead of `hours-55-35_tarot` on 2026-08-16 — it targets
the same leak for ~3× the value and can start immediately.
**Queue entry:** `docs/v1-test-queue.md`.

Everything needed to build this is in this file. It does not depend on any artifact URL.

---

## 1. What and why

The V1 close is 8 hardcoded messages, 132 words, in `handlePermission` (`useConversation.ts`).
Measured on Evelyn's own words, a session is **80% reading : 20% offer**, and the
commercial turn lands 63% of the way through.

**47–52% of non-buyers stop at PITCH** — the single biggest in-funnel leak, worth
~$8,117/30d at 25% recovery.

Against a standard close checklist, five of seven slots are empty: no mechanism, no price
justification, one line of proof, no objection pre-handling. Arm B fills **all five**.

**Honest limit:** buyers and non-buyers have the *same* 80:20 ratio today, so the data does
not show a thicker close converts better. It shows the offer is thin and the biggest leak
sits where a thin offer would hurt. This is a hypothesis, not a proven fix.

---

## 2. Experiment definition

```jsonc
{
  "key": "v1_close_depth_2026",
  "name": "Close depth — 140 words vs ~365",
  "status": "draft",              // ships OFF; nothing changes until started
  "subjectType": "email",          // assigned at lead capture, like v1_order_bump_2026
  "variants": [
    { "key": "A", "weight": 50, "payload": {} },
    { "key": "B", "weight": 50, "payload": { "close": "deep" } }
  ],
  "scope": { "funnel": ["v1-tarot"], "freezeAssignment": true },
  "conversion": { "type": "v1_main_funnel", "targetN": 7200, "windowDays": 7 }
}
```

**Recompute `targetN` at start** — it moved 6,800 → 7,300 → **7,200** in two days as
traffic shifted. `plan-live.mjs --live --size --effect 30 --metric pitch-paid`.
The 7,200 in the SQL was measured on 2026-08-16: 123/1258 = 9.8% pitch→paid over 7d.

| Parameter    | Value                                                                                       |
|--------------|---------------------------------------------------------------------------------------------|
| Primary      | Revenue per 1,000 leads (main + bump + U1 + U2) — never buy-rate                             |
| Guardrail 1  | Pitch → paid. The mechanism; this is what should move if it works                            |
| Guardrail 2  | Upsell-1 take. A longer close must not buy the front end at the back end's cost              |
| Detectable   | **+30% on a 9.8% baseline. A +20% effect will NOT be visible** (15,600 exposures / ~44 days against 7,200 / ~21) — state this before starting |
| Looks        | **Two only**, at 3,600 and 7,200 exposures. Pre-registered. `diagnose --tally` counts them   |
| Stop early   | Upsell-1 take drops >20% relative in arm B                                                   |

---

## 3. Code changes — AS BUILT

The plan said four edit points. It took seven, because the arm needs a resolver of its
own (the framework has no generic "assign at lead capture" call — every V1 test has a
named resolver) and because the admin API refuses `v1_main_funnel` for an unlisted key.

| # | File | What |
|---|------|------|
| 1 | `client/src/types/chat.ts` | `closeDepth?: 'deep'` on `UserData` — the field the pitch reads |
| 2 | `shared/types.ts` | the same field on the server-side `UserData`, because the client posts the whole object to `/api/chat`. No prompt builder reads it |
| 3 | `server/lib/experiments.ts` | `V1_CLOSE_DEPTH_EXPERIMENT_KEY` + `resolveV1CloseDepth()`, shaped like `resolveV1Bump` |
| 4 | `server/lib/priceVariant.ts` | resolve + log the exposure inside `assignVariantIfMissing`, **above** the price idempotency guard, and return `closeDepth` on both paths |
| 5 | `server/routes.ts` | emit `closeDepth` on the `/api/lead` response, only when `deep` |
| 6 | `server/routes/admin/experiments.ts` | add the key to `V1_MAIN_FUNNEL_KEYS`, or create/start rejects the conversion type |
| 7 | `client/src/hooks/useConversation.ts` | capture it from the lead response · the `?close_depth=deep` preview · the pitch branch |

Plus `server/lib/closeDepth.test.ts` (9 tests) and the creation SQL.

### 3.1 Why the subject is `hashEmail(email)`, not the raw address

Every other email-keyed V1 test passes the raw address to `assign()` and hashes only at
`logExposure`. This one must not, because it is the first email-keyed test to set
`scope.freezeAssignment` — and the freeze looks the subject up in
`experiment_exposures.subject_id`, which stores the **hash**. Passing the raw address
would miss every time, so the freeze would silently degrade to plain re-bucketing and a
weight edit could move a woman who had already read the other close. Locked by a test.

### 3.2 Why it is resolved above the price idempotency guard

Same reason as the commitment gate and the order bump: below the guard, an arm can only
ever be drawn for an email with no stored price, so every returning visitor would land in
control. Safe above it because this arm is the purest UI-only one of the three — it
changes which chat bubbles are sent and touches no price, line item or CTA.

### 3.3 The pitch branch

`handlePermission`, `useConversation.ts`. `deep` is `chat.userData.closeDepth === 'deep'`.
**Arm A is byte-identical** — verified by extracting all 20 message literals from the
control path at `HEAD` and confirming each survives verbatim.

`deep` composes with the existing `woven` arm rather than overriding it (woven keeps its
naming line, deep swaps the ritual body). They cannot co-occur in live traffic — woven is
fb-palm-scoped, deep is fb-tarot — but both preview overrides can be set at once.

The price and objection blocks are additionally gated on `pitchPrice === 35`, because
their copy says "Thirty-five" in words. Inside this test that guard never fires (v1-tarot
is the fixed `35_tarot` price); it exists so the `?close=55` preview and any future
funnel-wide rollout can't put two different prices in one close.

---

## 4. Arm B copy — 25 messages, 431 words (measured, as built)

Every message stays inside Evelyn's 25-word limit — checked mechanically over the whole
pitch region; the only line above it is a pre-existing message on the retired
sliding-close arm. `${firstName}` interpolates as today.

```ts
const deep = chat.userData.closeDepth === 'deep'

// ── NEW BLOCK: mechanism. Why the ritual works — Hopkins' reason-why.
// Without it every psychic's ritual sounds identical.
if (deep) await sendBotMessages([
  "What sits on you isn't a mood, dear. It's a pattern. And a pattern holds by staying unnamed.",
  "That's the whole of it. Nobody has ever said this thing out loud to you, so it kept its grip.",
  "Naming it precisely is what loosens it. Not wishing. Not hoping. Naming.",
])

// ── Step 3, ritual: 2 messages interleaved into the existing 3.
await sendBotMessages(deep ? [
  `Tonight, I'll enter a deep meditative state and focus entirely on your energy field, ${firstName}.`,
  "I go back to the hour it took hold. I trace what feeds it, sever that thread, and seal where it entered.",
  "It takes 2-3 hours of concentrated work. It drains me... but for those who are ready, it's worth it.",
  "A thing named properly doesn't grow back, dear. That's why I do it slowly.",
] : [
  `Tonight, I'll enter a deep meditative state and focus entirely on your energy field, ${firstName}.`,
  "I'll trace the roots of this block, sever its hold, and seal the clearing so it can't return.",
  "It takes 2-3 hours of concentrated work. It drains me... but for those who are ready, it's worth it.",
])

// ── Step 4, deliverable: "5-7 pages" is a size. This makes it a promise.
await sendBotMessages(deep ? [
  "Within 24 hours, you'll receive a personalized 5-7 page reading via email.",
  "Page one is what I found — the exact shape of it, and the hour it first took hold.",
  "Page two is what I cleared, and what resisted me. I tell you both. I don't tidy it up.",
  "Then the part most women read twice. Your next thirty days, set out week by week.",
  "What to expect in week one. What shifts by week three. The one sign that tells you it's working.",
  "And three steps. Small ones — things you can do on a Tuesday, not a mountain to climb.",
] : [
  "Within 24 hours, you'll receive a personalized 5-7 page reading via email.",
  "It will show you exactly what I found, what I cleared, and 3 specific steps for the next 30 days.",
])

// ── Step 5, price: the existing 3 messages, UNCHANGED in both arms,
//    preceded in arm B by the price-justification block. See §5.

// ── NEW BLOCK: proof. Makes the result small and believable rather than grand.
if (deep) await sendBotMessages([
  "It rarely arrives how they expect. It's small — sleeping through the night. Not reaching for the phone.",
])

// ── NEW BLOCK: objections. The two she ACTUALLY raises — see §6.
if (deep) await sendBotMessages([
  "Two things women say to me here. I'd rather answer them now than have you sitting with them.",
  "The first is money, dear. I know. Thirty-five is not nothing when the month is already thin.",
  "So don't decide by what you can spare. Decide by what another year of this costs you.",
  `The second is "let me think about it." That's fair. But I read what's live tonight, not what's cooled by Friday.`,
  "Waiting doesn't keep it safe for you. It just means I'd be reading a fainter thing.",
])

// ── Step 6, LLM vision close: unchanged in both arms.
```

---

## 5. The price block — INCLUDED, since this now runs first

Three messages, placed **before all three existing price messages** (not between them):

> §4 said "preceded by" and this section originally said "immediately before the
> guarantee line" — two different placements. Built as the first: it builds plain →
> sacred, where the other restarts a block that has already named the price.
> "The sacred offering is $35 — a declaration…" then closes it.

```ts
if (deep) await sendBotMessages([
  `Now the offering, ${firstName}. Thirty-five dollars. Let me tell you plainly why it's that and not more.`,
  "Because the women who need this most are usually the ones who've already paid a great deal to be told nothing.",
  "Thirty-five keeps me in candles and keeps this honest. It's a marker that you're ready. Not a wall.",
])
```

It was cut in the first draft because `hours-55-35_tarot` owned that beat. Close depth now
runs first, so it owns the beat until it resolves and arm B is the full five-block
treatment — **~365 words, 26 messages**.

🔴 **If the order is ever reversed again, cut this block before starting, not during.**
Changing arm B's content mid-flight invalidates everything collected before the change.

**Objection wording must never name the downsell.** Pre-announcing that $25 exists has been
tested and lost — the sliding-scale arm was retired 2026-07-22 for exactly this. The block
argues *worth*, never discount. The $25 downsell stays reactive.

---

## 6. Evidence behind each block

| Block       | Basis                                                                                                                                                              |
|-------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Mechanism   | Hopkins' reason-why. Absent today — the ritual is asserted, never explained                                                                                         |
| Deliverable | Ogilvy's specificity. "5-7 pages" is a size, not a promise                                                                                                          |
| Proof       | Believable beats impressive. Reframes the result as small and concrete                                                                                              |
| Objections  | **Mined, not invented.** 517 non-buying sessions: price 23–32% in every hook family, "let me think" 3–8%. *"What if I feel nothing?"* was said **zero times** — an earlier draft answered it |

🔴 **Weakest-evidenced block: objections.** Only ~30% of non-buyers say anything at all at
the offer; the other 70% read it and leave. Pre-handling reaches the ones who would have
spoken *unless* the silent majority hold the same objection — which is the standard premise
of pre-handling, and **a premise, not a finding**. If arm B has to shrink, cut this first.

---

## 7. Verification

Done at build time (2026-08-16):

- [x] Arm A is **byte-identical** — all 20 control-path message literals at `HEAD` survive
      verbatim; the only removed lines are structural.
- [x] Every arm-B message ≤ 25 words (checked over the whole pitch region).
- [x] `plan-live.mjs --live --beats price,objections` → "would collide with: nothing running".
- [x] `targetN` recomputed on the live baseline → 7,200.
- [x] Look schedule (3,600 / 7,200) written into `docs/v1-test-queue.md` before any exposure.
- [x] Preview override `?close_depth=deep` implemented, mirroring `?clearing=woven`.
- [x] Ship-dark invariant covered by `server/lib/closeDepth.test.ts` — no test / draft /
      out-of-scope funnel / no email all yield today's close and enrol nobody.
- [x] **The pitch-arrival detector still fires on both arms.** `PRODUCT_LINE`
      (`v1-funnel-live-audit/scripts/lib/live-db.mjs`) is what turns a stored transcript
      into "she reached the offer", and pitch→paid is the primary guardrail. Arm B drops
      one of its alternatives ("trace the roots of this block") but still matches on four
      others — "deep meditative state", "5-7 page", "personalized … reading via email",
      "sacred offering", "30-day guarantee". No arm is under-counted at the pitch.
- [x] `npx tsc --noEmit` clean on every touched file; `npm run test:experiments` 57/57.

Still to do, in order:

- [ ] Deploy.
- [ ] Run the creation SQL on **dev**, walk `?close_depth=deep` end to end, then run it on prod.
- [ ] Re-check `targetN` against that day's baseline.
- [ ] Start it in `/admin/experiments` and confirm the post-start checks in the SQL file
      (~50/50, every exposure `funnel=v1-tarot`, no exposure missing a `conversationId`).

---

## 8. Held for later — root-lander fix

Arm B is scoped to fb-tarot, which is 96.6% love bucket, so this does not apply to the test.
**If arm B wins and ships funnel-wide**, root is only 45% love and one line breaks:

| | |
|---|---|
| Breaks on money/purpose | "It's small — sleeping through the night. **Not reaching for the phone.**" |
| Neutral replacement | "It's small — sleeping through the night. **A morning that starts without the same weight on it.**" |

The close block has no bucket awareness at all (`useConversation.ts:1810–1855` interpolates
only `firstName`), which is a pre-existing gap this proposal neither creates nor fixes.
