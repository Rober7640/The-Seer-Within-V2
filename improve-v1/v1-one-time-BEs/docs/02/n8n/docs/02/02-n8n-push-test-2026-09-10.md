# 02: n8n push and test — 10 September 2026

## Scope and gate

The operator authorized pushing and testing the actual workflow. The main workflow remains inactive. The test drive removes customer delivery and disables storage; it exercises the actual draw, house loop, joiner, grader, HTML and PDF service. A green execution proves operation, not acceptable prose or an 8/10 customer experience.

- [x] Rebuild from generator/spec; no hand-edited workflow JSON.
- [x] Pass promise checks, draw checks across 9,000 seeds, and architecture checks across 2,000 orders.
- [x] Push main workflow `5QkhGbpsusvIfh6j`; response confirms inactive.
- [x] Push v2 test workflow `sKwnqW5zGAxt8ghY` and verify its node parameters before execution.
- [x] First activation produced executions `30649` and `30650`; the old 70-second arming window admitted two ticks. Schedule subsequently disarmed.
- [x] Inspect the failure in actual generated output.
- [x] Fix the takeaway validator's false positive; preserve date and claim rejection.
- [x] Add a regression check using the actual failed takeaway and seven negative controls.
- [x] Rebuild and rerun all required checks after the fix.
- [x] Corrected activation: execution `30652` completed through PDFShift; all ten authored core nodes match the current local build.
- [x] Remote status read confirms main and test workflows inactive; no delivery nodes run.
- [x] Complete corrected report rendered from its actual node-10 HTML and submitted for independent blind review.
- [x] Fresh blind reviewer assessed the corrected report with the customer-experience scorecard; see final result below.
- [ ] Whole report meets 8/10: not yet established.

## First execution: a validator defect

Execution `30649` stopped at node 4c in house 6. It rejected:

> Judge the week by its mixture and residue, not its felt intensity.

The check treated any occurrence of `week`, `day` or `month` as a dated prediction. This is an ordinary reflection, not a deadline. The generator now rejects date constructions and prohibited claims while allowing an unqualified duration noun. The regression script executes the built parser; it does not merely grep the source.

The rerun preserves test order `cs_test_n8n_testdrive_02_v2` and arc v2. This is a workflow smoke test, not a score comparison with the earlier local editorial order `cs_phase3_v2_20260910_d`.

## Model provenance and the writing decision

Configured writer, joiner and grader: `claude-opus-5`. The Anthropic requests include server-side fallback. Of the six writer responses in execution `30649`, five reported `claude-opus-5` and one reported `claude-opus-4-8`. Configured model and actual returned model must therefore be recorded separately.

No defensible conclusion that Opus 5 writes better than GPT follows from this run. Earlier prompt edits and local editorial repairs are confounding factors. Keep Claude provisionally; choose a GPT challenger through a blind test with the same explicit order, draw, architecture, obligations and evaluation contract. Record actual returned models, runtime, usage and any fallbacks. Use three houses to screen, then a whole report to confirm. A model change alone does not establish 8/10.

The latest local editorial repair stage is still experimental and is not installed in n8n. Scores from repaired excerpts cannot be attributed to this deployed workflow.

## Evidence files

- Generator: `scripts/build-02-n8n.py`.
- Regression: `scripts/check-02-takeaways.mjs`.
- Execution extraction: `scripts/read-02-run.mjs` now records model provenance, compares executed nodes with the local build, and selects the latest assembly/grade after regeneration.
- Test driver: `scripts/testdrive-02-n8n.py`.

## Delayed execution and blind result

Execution `30650` started at 06:28:29 UTC and finished at 06:50:47 UTC: 22 minutes 18 seconds. It belongs to the first activation, not the validator-fix rerun. The original driver mistakenly reported it as the rerun because its ID was newer than the baseline and it appeared late. The extraction instrument caught the mismatch: only node 4c differed from the corrected build.

- [x] Save actual n8n HTML, draw, report, grader result and model provenance.
- [x] Render the saved node-10 HTML: 35 pages, 13 images, eight takeaways, no broken image requests.
- [x] Blind full-PDF review: `02-n8n-blind-review-30650.md`.
- [x] Driver now checks activation timestamps and executed parameters before claiming success.
- [x] Generator uses an explicit clock-aligned minute tick; driver disarms three seconds after its precomputed tick instead of waiting 70 seconds. Timing-window arithmetic checked locally; updated trigger saved to the inactive v2 test workflow.
- [ ] The revised schedule driver has been exercised on n8n; do not claim this from local arithmetic.

The first generation failed repetition (line 13, automated 6.75), causing the full regeneration. The final reading is 8,216 words. It used two generations (28 model calls): 27 responses reported Opus 5 and one reported Opus 4.8. Its automated prose score was 8.45 and recommendation 8, yet its hard verdict failed line 14 because House 5 was 803 words. The blind reviewer scored readability 6, trust 5, humanness 6, relevance 6, desire to continue 6 and recommendation 5. **Not ready to release.**

The five ranked defects are inconsistent arrival/renewal and recognition timing; invented supporting biography; overbroad passivity advice; repetitive explanatory scaffolds; and uneven pagination. The observation gift and reader-led reflections are strengths.

## Next work, in order

1. Establish one structured record of branch, recognition event and relative-week origin. Validate all later houses and the close against it. Measure zero contradictions and zero event/date drift in the actual report.
2. Preserve accepted passages and repair only the failing house or close. Keep the 500–800 limit; do not erase it to turn a red result green. Measure paid calls and elapsed minutes per repair, and confirm unchanged passages remain unchanged.
3. Screen Claude and a GPT challenger on the same three houses, explicit order and frozen context. Blind-score readability, respect, teaching and desire to continue; separately count contract omissions and unsupported additions. Record actual models and fallback use. No model superiority claim before this comparison.
4. Confirm the winner in a complete PDF, tighten repetition, and inspect pagination. Retain the independent operator gate.

The local editorial pass is still absent from the deployed workflow. This test does not transfer the earlier repaired-excerpt scores to n8n.

## Corrected execution — final test evidence

Execution `30652` started at 06:41:19 UTC and finished at 07:03:52 UTC (22 minutes 34 seconds). Core draw/writer/parser/joiner/grader/renderer parameters match the current local build, including the fixed takeaway validator. The browser execution view explicitly showed **no active executions**; both saved workflows are inactive. No further full generation was started.

The first generation failed lines 8, 13 and 14 (automated score 5.85). After one full regeneration, the final reading passed the hard rubric and scored 8.05 automatically, recommendation 8. It is 8,382 words, rendered locally from the exact n8n node-10 HTML as 35 pages, with 13 intact images and eight bold takeaways. PDFShift itself succeeded in n8n; the locally saved PDF is a Chromium rendering of that same HTML, not a download of PDFShift's binary.

There were 28 calls: 26 Opus 5 and two Opus 4.8 fallback responses. Both fallback responses occurred in the first, discarded house generation; **the final report generation, join and grade all returned Opus 5**. Fallback remains enabled in the deployed configuration.

The schedule timing change was saved afterwards, inactive. Its timing arithmetic and the driver's timestamp/build guards were tested locally against recorded executions. The timestamp check admits a late recorded start and rejects the older execution; it no longer relies on an arbitrary upper limit after activation. Do not label the revised scheduling mechanism runtime-proven until it has its own future test.

Current artifacts: `n8n-02-reading-30652.pdf`, `n8n-02-reading-30652.md`, `n8n-02-draw-30652.json`, `n8n-02-audit-30652.json`, `n8n-02-provenance-30652.json`, and `02-n8n-blind-review-30652.md`.

### Final blind verdict for the corrected report

**REVISE AND REVIEW AGAIN.** Readability 7/10; trust 5/10; humanness 6/10; personal relevance 6/10; desire to continue 6/10; recommendation 5/10. These are simulated editorial scores, not customer NPS. No whole-report 8/10 claim is justified.

The reviewer found that the required predictive answers are present. The strongest remaining problems are invented interaction history in House 12, one-chance pressure in House 5, unsupported persistence instructions, repeated teaching structures, visible Markdown emphasis marks and uneven pagination. Keep the useful card-specific teaching and observation gift.

The review also flags the cover's calendar issuance date under a literal reading of the no-calendar-dates rule. Treat that separately from dated predictions: the central timing restriction concerns the reading's forecast, and this formatting observation is not grounds to weaken that restriction or to alter the independent review's prose scores.

**Model decision:** do not claim Opus 5 is superior to GPT. The latest final report was written entirely by Opus 5 but still misses the operator's quality bar. Keep the current configuration provisional and inactive. Screen a GPT challenger with the same three-house experiment before deciding the writer; current official OpenAI documentation identifies GPT-5.6 Sol as a flagship for complex professional work, which makes it a candidate, not evidence of superior prose. [Official model catalog](https://developers.openai.com/api/docs/models).

**Scope limit:** this test validates generation through PDF. Storage upload and customer delivery were excluded. The local editorial repair mechanism has not been installed in production. No commit or live activation was performed.
