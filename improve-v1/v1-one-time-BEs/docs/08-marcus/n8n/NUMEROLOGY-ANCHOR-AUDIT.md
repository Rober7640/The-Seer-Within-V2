# Numerology-anchor audit — fulfillment draft

Date: 2026-09-13

## Report inspected

`output/pdf/marcus-yan-wei-higher-calling-test.pdf` is a 12-page PDF created on 2026-09-10. Its footer says `STAGE 1 MANUAL TEST`, so it was not generated by the fulfillment draft uploaded on 2026-09-13.

The concern it exposed is valid:

- the report never states `Life Path 4 — The Builder`;
- it uses the vague phrase “your profile shows” without giving the customer a recognizable foundation;
- The Lovers receives a long standalone treatment and can displace the Builder identity;
- the report does not prove that Expression 6 and Personality 1 shaped its synthesis;
- no deterministic gate would stop a production backend from returning the same incomplete structure.

## Corrected contract

The production brief must contain:

- customer-visible Life Path number, archetype, core statement, strengths, and growth edges;
- private Expression and Personality numbers;
- exactly one pinned canon entry for each role, including key, version, hash, and passages;
- an Expression-derived personal card that matches the saved profile;
- the question, edition theme, saved spread, and saved buyer draw.

The customer report must state Life Path and archetype plainly near the beginning and show a dedicated numerology-foundation section before the personal card. Expression and Personality remain private evidence. The personal card is secondary and explains how the Life Path may approach the question.

## Implementation

- The local `build-brief` adapter now calculates the full profile from the saved birth name and date of birth, selects `LPn`, `EXn`, and `PEn` from canon v1, and checks that the saved personal card matches Expression.
- The local grade fails unless the report preserves the Life Path number/archetype and its private Expression/Personality evidence.
- The PDF renderer requires and displays the Life Path foundation before the personal card.
- The fulfillment workflow now has an explicit `Require numerology anchors` code gate between `Build canon-backed numerology brief` and report writing.
- The dedicated cloud draft was updated in place from 47 to 48 nodes and remains inactive with `enabled: false`.

## Verification

- Profile `Chue Yan Wei`, `1985-04-13` produced `LP4`, `EX6`, and `PE1`.
- Customer-visible foundation: `Life Path 4 — The Builder`.
- Personal card: The Lovers, kept as the secondary lens.
- Private keys `LP4`, `EX6`, and `PE1` are stored in report evidence but do not appear in customer PDF text.
- Three local fulfillment tests pass, including PDF generation and exact 4/6/1 assertions.
- Two workflow contract tests pass, including fail-closed anchor routing and the Chatterbox Turbo schema.
- The revised three-page structural PDF rendered without clipping, overlap, or broken page furniture.

Cloud workflow: <https://ezyabsorb.app.n8n.cloud/workflow/UJamB32MGlNKdoEW>
